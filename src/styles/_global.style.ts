import { css } from '@emotion/core';
import { WHITE } from './_colors.style';

export default css`
  body {
    margin: 0;
    padding: 0;
    font-family: 'Muli', 'Fira Sans', sans-serif;
    background: ${WHITE};
  }
  * {
    box-sizing: border-box;
  }
`;
